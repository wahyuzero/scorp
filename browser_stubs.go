// +build nobrowser

package main

func initVault() {
	// No-op when browser tools are disabled
}

func cleanupStaleBrowserSessions() {
	// No-op when browser tools are disabled
}

func initMonitor() {
	// No-op when browser tools are disabled
}
